import React from 'react';

const BinaryOption = () => {
    return (
        <div>
            <button>yes</button>
            <div></div>
            <button>no</button>
        </div>
    );
};

export default BinaryOption;